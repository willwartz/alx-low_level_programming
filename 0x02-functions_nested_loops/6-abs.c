#include "main.h"
/**
 * main - Entry Point
 *
 * _putchar - Print to output
 *
 * Return: Always 0
*/

int abs(int n)
{
    if (n >= 0)
    {
        return(n);
    }
    else
    {
        return(n * -1);
    }
}

